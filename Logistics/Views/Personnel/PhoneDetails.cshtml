@model Logistics.Entities.Contact.Phone

@{
    ViewBag.Icon = "pe-7s-call";
    ViewBag.Title = "Phone Details";
    ViewBag.Message = "Add, edit and view Phone details.";

    Html.RenderPartial("~/Views/Shared/Luna/_PageHeader.cshtml");
}

@section CustomJs{
    <script src="@Url.Content("~/Scripts/Personnel/phone.js")" type="text/javascript"></script>
}

@using (Html.BeginForm("PhoneDetails", "Personnel", FormMethod.Post, new { @id = "frmPhone" }))
{
    @Html.HiddenFor(model => model.Id)
    @Html.HiddenFor(model => model.PersonId)

    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-filled">
                <div class="panel-body">
                    <h4 class="m-b-xs">@(Model.Person.FullName)</h4>
                    <hr />
                    <div class="form-horizontal">
                        <div class="form-group">
                            <label for="TypeLookupId" class="col-sm-2 control-label">Phone Type</label>
                            <div class="col-sm-10">
                                @Html.DropDownList("TypeLookupId", ViewBag.ListOfAddressTypes as IEnumerable<SelectListItem>, "", new { @class = "form-control", required = "required" })
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="Number" class="col-sm-2 control-label">Number</label>
                            <div class="col-sm-10">@Html.TextBoxFor(model => model.Number, new { @class = "form-control", required = "required" })</div>
                        </div>
                        <div class="form-group">
                            <label for="Ext" class="col-sm-2 control-label">Ext</label>
                            <div class="col-sm-10">@Html.TextBoxFor(model => model.Ext, new { @class = "form-control" })</div>
                        </div>
                        <hr />
                        <div class="form-group">
                            <div class="col-sm-2"></div>
                            <div class="col-sm-10">
                                <a id="btn-close" class="btn btn-default" title="Save">Close</a>
                                <button id="btn-save" class="btn btn-accent" title="Save">Save</button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
}